@import "../../styles/variables.pcss";

.profile-link {
    align-self: flex-end;

    color: $primary-gray;
    line-height: 100%;
    font-weight: 500;
    font-size: 12px;
    text-decoration: none;
    transition: 0.175s;
    margin-bottom: 23px;

    &:hover {
        color: #5f5f5f;
    }
}
